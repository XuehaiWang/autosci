name: ask_user
description: Ask the user a question and wait for their response. Use this when you need clarification, confirmation, or a
  decision from the user before proceeding.
toolset: agent
input_schema:
  type: object
  properties:
    question:
      type: string
      description: The question to ask the user
  required:
  - question
