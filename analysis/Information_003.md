# Information_003 — 9%

## 任务概述
复现 DIDS-MFL（解耦入侵检测系统 + 元特征学习）：在 NF-UNSW-NB15-v2 上少样本 F1 提升 71.91%-125.19%（vs 14 个基线），NMI 指标，并与 GPT-3.5/4 对比推理时间。

## 得分明细
| item | 得分 | 权重 | 类型 | 失分原因摘要 |
|------|------|------|------|------------|
| [0] | 5/100 | 0.15 | image | 需要特征分布纠缠可视化（per-attack F1 方差、相关热图），agent 只有聚合条形图 |
| [1] | 0/100 | 0.2 | image | 需要统计/表示解耦和图扩散的可视化（类似论文 Fig.7-9），agent 没有生成 |
| [2] | 25/100 | 0.3 | text | 需要 vs 14 个基线的 F1/NMI 改进，agent 只对比了 1 个基线，无 NMI，无多次运行统计 |
| [3] | 3/100 | 0.2 | image | 需要消融实验（SD/RD/MLGRAND 组件）+ t-SNE + 相关热图，agent 只有简单条形图 |
| [4] | 0/100 | 0.15 | text | 需要 vs GPT-3.5/4 的 F1 和推理时间对比（100 个流量样本），agent 完全没有做 |

## 失分根因
1. **基线数量严重不足**：checklist 要求与 14 个基线对比，agent 只对比了 1 个，且没有报告 NMI 指标和多次运行的方差统计。
2. **可视化类型错误**：需要展示特征解耦机制的专业可视化（t-SNE、相关热图、图扩散动态），agent 只生成了通用的聚合条形图。
3. **缺少 LLM 对比实验**：完全没有做 GPT-3.5/4 对比，这是论文的一个重要贡献点。

## 数据/方法缺口
- **方法偏差**：task_plan 没有提取出"14 个基线"、"NMI 指标"、"GPT 对比"这些具体要求
- **实现不完整**：消融实验（SD/RD/MLGRAND 三个组件）完全缺失
